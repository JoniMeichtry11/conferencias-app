rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allowing read/write access for development purposes. 
      // In a production app, you should implement authentication and granular rules.
      allow read, write: if true;
    }
  }
}
